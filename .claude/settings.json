{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "$json = [Console]::In.ReadToEnd() | ConvertFrom-Json; $f = $json.tool_input.file_path; if ($f) { git add $f; git commit -m 'auto: save changes'; git push } 2>$null",
            "timeout": 30,
            "statusMessage": "Pusher til GitHub..."
          }
        ]
      }
    ]
  }
}
